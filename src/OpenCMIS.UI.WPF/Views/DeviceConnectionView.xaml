<UserControl x:Class="OpenCMIS.UI.WPF.Views.DeviceConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:OpenCMIS.UI.WPF.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel MaxWidth="500">
        <TextBlock Text="Device Connection" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" />

        <GroupBox Header="Scan &amp; Connect" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <ComboBox ItemsSource="{Binding AvailablePorts}"
                          SelectedItem="{Binding SelectedPort}"
                          IsEnabled="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                          materialDesign:HintAssist.Hint="Select Port"
                          Margin="0,0,0,10" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Content="Scan"
                            Command="{Binding ScanPortsCommand}"
                            IsEnabled="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                            Margin="0,0,5,0" />

                    <Button Grid.Column="1"
                            Content="Connect"
                            Command="{Binding ConnectCommand}"
                            Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                            Margin="5,0,0,0" />
                </Grid>

                <Button Content="Disconnect"
                        Command="{Binding DisconnectCommand}"
                        Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}"
                        Margin="0,10,0,0" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Device Info" Padding="10"
                  Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock>
                    <Run Text="Vendor: " FontWeight="Bold" />
                    <Run Text="{Binding VendorName}" />
                </TextBlock>
                <TextBlock Margin="0,5,0,0">
                    <Run Text="Part Number: " FontWeight="Bold" />
                    <Run Text="{Binding PartNumber}" />
                </TextBlock>
                <TextBlock Margin="0,5,0,0">
                    <Run Text="Serial Number: " FontWeight="Bold" />
                    <Run Text="{Binding SerialNumber}" />
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <TextBlock Text="{Binding StatusMessage}" Foreground="Gray" Margin="0,10,0,0" />
    </StackPanel>
</UserControl>
